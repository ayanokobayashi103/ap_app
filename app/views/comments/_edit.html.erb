<%= form_with(model: [review, comment]) do |form| %>
  <div class="field">
    <%= form.text_area :content, id: "comment_content_#{review.id}" %>
    <%= form.label :content %>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
